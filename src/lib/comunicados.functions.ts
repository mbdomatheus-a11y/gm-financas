import { createServerFn } from '@tanstack/react-start'; import { z } from 'zod'; import { requireSupabaseAuth } from '@/integrations/supabase/auth-middleware';
export const meusComunicados=createServerFn({method:'GET'}).middleware([requireSupabaseAuth]).handler(async({context})=>{const{supabaseAdmin}=await import('@/integrations/supabase/client.server');const db=supabaseAdmin as any;const{data,error}=await db.from('comunicados').select('id,titulo,mensagem,exige_aceite').eq('ativo',true).or(`expira_em.is.null,expira_em.gt.${new Date().toISOString()}`);if(error)throw new Error(error.message);const{data:aceitos}=await db.from('comunicado_aceites').select('comunicado_id').eq('user_id',context.userId);const ids=new Set((aceitos??[]).map((x:any)=>x.comunicado_id));return(data??[]).filter((x:any)=>!ids.has(x.id));});
export const aceitarComunicado=createServerFn({method:'POST'}).middleware([requireSupabaseAuth]).inputValidator((v:unknown)=>z.object({id:z.string().uuid()}).parse(v)).handler(async({data,context})=>{const{supabaseAdmin}=await import('@/integrations/supabase/client.server');const{error}=await(supabaseAdmin as any).from('comunicado_aceites').upsert({comunicado_id:data.id,user_id:context.userId});if(error)throw new Error(error.message);return{ok:true as const};});
